node_modules
dist
.env
.env.local
.DS_Store
*.log

# Credenziali OAuth (NON committare mai questi file)
client_secret*.json
credentials*.json
*service-account*.json
*.xlsm
*.xlsx
*.xls

# MCP project-level config — contiene PAT Supabase, va escluso
.mcp.json

# Export di backup del DB (contengono dati personali: nomi, ferie, email).
# Il repo e' PUBBLICO → non devono MAI essere committati. Salvati fuori dal
# repo (vedi scripts/export-backup.mjs).
backups/
*.backup.json
